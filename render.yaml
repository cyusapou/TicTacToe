services:
  # Single deployment: Server + Frontend
  - type: web_service
    name: tictactoe
    rootDir: .
    buildCommand: npm install && npm run build && cd server && npm install
    startCommand: cd server && npm start
    envVars:
      - key: NODE_ENV
        value: production
